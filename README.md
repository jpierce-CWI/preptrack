Pre Reqs to Run dev server
Node.js 20.0+
pnpm (use npm install -g pnpm) (-g installs globally)


Unzip and in terminal of project ropot run pnpm install

run "npm run dev" in terminal

PrepTrack Features

Keep track of whats been prepped, active jobs, and past jobs. Utilizies indexDB to store locally and has the abillity to import/export JSON files as an extra storage method.